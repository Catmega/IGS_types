After test, Xopera6.9 cannot set inputs for custom Interface and operations.

Using Validation interface needs to manually set 'log_file_loc' in each ansible playbook that the validate operation points to.